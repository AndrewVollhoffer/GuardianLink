<%= form_with(model: ngo) do |form| %>
  <% if ngo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ngo.errors.count, "error") %> prohibited this ngo from being saved:</h2>

      <ul>
        <% ngo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
